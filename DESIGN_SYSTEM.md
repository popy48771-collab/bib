# デザインシステム

このアプリの画面を触るときの規則。**迷ったらこの文書に従う。**

- 実装は [`src/ui/styles.css`](src/ui/styles.css) の1枚に集約されている。トークンはその冒頭の `:root` のみ
- 依頼原文（方針が食い違ったときの根拠）は [docs/DESIGN_RULES.md](docs/DESIGN_RULES.md)
- **値の出所はデジタル庁デザインシステムの公式実装。** 目分量で決めた値は無い

| 出所 | 何を取ったか |
|---|---|
| [`@digital-go-jp/design-tokens`](https://www.npmjs.com/package/@digital-go-jp/design-tokens) 2.0.1 `dist/tokens.css` | 色のプリミティブ、フォントサイズ、行高、角丸、エレベーション |
| [`digital-go-jp/design-system-example-components-html`](https://github.com/digital-go-jp/design-system-example-components-html) `src/global.css` | 意味づけ（error / success / key）、リンク、`:focus-visible` |
| 同リポジトリ `src/components/*/*.css` | ボタン・入力欄・通知バナー・区切り線の寸法と状態 |

`styles.css` の各トークンには、対応する公式トークン名をコメントで併記してある。値を変えるときは公式側の名前を必ず確認する。

**ロゴ・紋章・政府公式サービスと誤認させる表現は使わない。** 借りるのは情報設計・アクセシビリティ・タイポグラフィ・フォーム・ボタンの設計原則と、その具体値だけ。

目指す印象は「公共的・誠実・正確・静か・実務的」。**「おしゃれ」「親しみやすい」「未来的」は目標にしない。**

---

## 1. トークン

**新しい色・角丸・影を画面側で足さない。** 必要になったらまず `:root` に追加し、この表も更新する。

### 色

| 変数 | 値 | 公式トークン名 | 用途 |
|---|---|---|---|
| `--color-bg` | `#f2f2f2` | `solid-gray-50` | ページ背景 |
| `--color-surface` | `#ffffff` | `neutral-white` | コンテンツ背景 |
| `--color-text` | `#1a1a1a` | `solid-gray-900` | 主文字・見出し |
| `--color-text-body` | `#333333` | `solid-gray-800` | 本文（通知の本体など） |
| `--color-text-sub` | `#4d4d4d` | `solid-gray-700` | 補助文字 |
| `--color-text-weak` | `#666666` | `solid-gray-600` | 薄い補助文字（出版社・ISBN など） |
| `--color-border` | `#666666` | `solid-gray-600` | 入力欄の枠 |
| `--color-divider` | `#949494` | `solid-gray-420` | 区切り線・領域の外枠 |
| `--color-primary` | `#0017c1` | `key-900` (`blue-900`) | 主要ボタン・現在地 |
| `--color-primary-hover` | `#00118f` | `key-1000` | 同 hover |
| `--color-primary-active` | `#000060` | `key-1200` | 同 active |
| `--color-link` | `#00118f` | `blue-1000` | リンク（ボタンより一段濃い） |
| `--color-link-hover` | `#0017c1` | `blue-900` | リンク hover（明るくなる） |
| `--color-error` | `#ec0000` | `semantic-error-1` (`red-800`) | エラー |
| `--color-success` | `#197a4b` | `semantic-success-2` (`green-800`) | 成功 |
| `--color-focus` | `#ffd43d` | `yellow-300` | フォーカスの内側の輪 |
| `--color-focus-outline` | `#000000` | `neutral-black` | フォーカスの外側の輪 |

不活性（`--color-disabled-*`）も公式に合わせてある。塗りボタンは `gray-300` の面に `gray-50` の文字、線ボタンと入力欄は白地に `gray-300`。**WCAG は不活性の部品をコントラスト要件の対象外としており、公式もこの値を使っている。**

**区切り線を薄くしすぎない。** DADS が `divider` / `table` / `list` で使う線は `solid-gray-420` だけで、`gray-100` や `gray-200` を線に使う例は無い。

**プライマリーは主要ボタン・リンク・現在地表示だけに使う。** 基本画面は白・薄いグレー・濃い文字で組む。
任意実行のボタンは `.button--neutral`（無彩色）にして、青を1画面1〜2か所に留める。

### 余白

`--space-1` 〜 `--space-8` = **4 / 8 / 12 / 16 / 24 / 32px**。この6段階以外を使わない。

- スマートフォンの左右余白: 16px（`.container`）
- コンテンツ最大幅: 720px（`--content-max`）
- 縦の間隔は `.stack`（子要素間 16px）と `.section`（32px）に任せる。個別に margin を足さない

### 形状

- 通常の角丸: `--radius` = **8px**（DADS の `button[md]` / `input` / `select` / `textarea` がいずれも 8px）
- `--radius-sm` = 6px（`button[sm]`）、`--radius-xs` = 4px（`button[xs]`・フォーカスの輪・進捗バー）
- **タグ以外でカプセル形状（`border-radius: 999px`）を使わない**
- 区切りは 1px の境界線と余白で表す

### 影

**通常のカード・一覧行に影を付けない。** 影は次の2つだけ。

| 変数 | 用途 |
|---|---|
| `--shadow-overlay` | 実際に前面へ浮くもの（モーダル等）専用。DADS の `elevation-1`。現状の画面では未使用 |

`box-shadow` は他に2か所で使うが、いずれも**影ではない**。

- フォーカスの輪（`0 0 0 2px` の黄）— 装飾ではなく可視化
- 通知バナーと状態表示の左端の色帯（`inset 8px 0 0 0`）— DADS のノティフィケーションバナー `color-chip` の実装手段
- 読取レーンの枠の外側の黒い細線（`0 0 0 1px`）— 白い背表紙に白い枠が重なると消えるため。これも可視化

### タイポグラフィ

`--font-family` はシステムフォント。**外部Webフォントを必須にしない。**

| 変数 | 値 | 用途 |
|---|---|---|
| `--text-body` | 16px / 1.6 | 本文 |
| `--text-page-title` | 24px / 700 / 1.4 | ページ見出し（`.page-title`, `h1`） |
| `--text-section` | 20px / 700 / 1.4 | セクション見出し（`.section-title`, `h2`） |
| `--text-subheading` | 17px / 700 | 小見出し（`.subheading`, `.record__title`） |
| `--text-note` | 14px / 1.5 | 補助情報（`.note`） |
| `--text-service-name` | 18px / 700 | ヘッダーのサービス名 |

- **14px 未満を使わない**（等幅の書き出しプレビューも 14px）
- 本文に 300 などの細いウェイトを使わない。英字の字間を広げない
- 見出しを不必要に巨大化しない
- 字送りは `--tracking` = `0.02em`。DADS が和文の可読性のため全体に入れているもので、これ以上広げない

---

## 2. 部品の規則

### ヘッダー・フッター

- ヘッダー: 白背景、上端に 4px のプライマリー、サービス名のみ。**ロゴ・キャッチコピー・イラストを置かない**
- **画面は1枚。** 読み取りと一覧が同じ画面で伸びていくので、ナビゲーションは置かない。一覧へは `#library` へのスクロールで移動する
- フッター: 書誌情報の出典、利用条件・データの取り扱いへのリンク、**国立国会図書館の公式サービスではない旨**

### ボタン

| クラス | 見え方 | 使う場面 |
|---|---|---|
| `.button--primary` | 単色の青、白文字 | その画面の主操作。1画面に1つ |
| `.button--secondary` | 白地・青枠・青文字 | 主操作と対になる操作（停止・コピー） |
| `.button--neutral` | 白地・灰枠・黒文字 | 任意実行の操作 |
| `.button--danger` | 白地・赤枠・赤文字 | 削除。必ず「削除」の語を伴わせる |
| `.button--compact` | 44px / 14px | 一覧行の副次操作 |

- 主ボタンは最小幅 96px・高さ 48px・角丸 8px（DADS の `button[data-size="md"]`）
- 文言は具体的な動詞にする（「カメラを開始」「再読み取り」「一覧に追加」「ファイルを保存」）
- **アイコンだけで意味を伝えるボタンを作らない**
- **hover では色だけでなく下線も出す。** 色の違いが分からない人にも反応が伝わるようにするため（DADS の指定）
- 不活性は `opacity` で薄めない。公式どおり色を差し替える
- `.button--compact` は実寸で 44px の高さを持たせる。公式は見た目 28〜36px に `::after` で 44px の当たりを足す方式だが、実寸の方が検査で担保しやすいのでこちらを採る

### フォーム

並びは **ラベル → 補足説明 → 入力欄** に統一する。

- `label` と入力欄を `htmlFor` / `id` で必ず結ぶ。ラベルは 17px 太字（DADS の `form-control-label[md]`）
- 入力欄は高さ 48px・内側余白 12/16・角丸 8px・枠 1px `--color-border`
- hover で枠を黒にする。**読み取り専用（`readonly`）は破線で示す**（DADS の `:read-only` の扱い）
- 排他選択は `.choice-list` の `input[type="radio"]`。行全体を `<label>` にしてタップ領域を確保する

### 一覧（書誌一覧）

**本ごとに独立した浮遊カードを並べない。** 白い `.record-list` を1つ置き、内側を 1px の境界線で区切る。

行の順序は固定する。

1. 書名（`.record__title`, 17px/700 — 最も強い）
2. 著者（`.record__authors`, 16px）
3. 出版社・出版年 → ISBN → シリーズ（`.record__meta`, 14px の補助情報）
4. 状態の説明文（`.record__state`）
5. 副次操作（`.record__actions`）

書影は 40px 幅の補助要素。**枠は常に確保する**（取得できた行だけ幅が出ると書名の頭が揃わない）。取得できなかった場所は白のままにし、灰色の四角を並べない。

**読み取った背表紙の画像（`.crop`）は、確認が要る行にだけ出す。** 候補を選ぶときに本棚まで見に戻らずに済むのが目的なので、確定した行に出しても場所を取るだけになる。画像は端末内にしか無く、保存できていないこともあるので、取れなければ黙って出さない。

### 通知・状態表示

`.notice` は DADS のノティフィケーションバナー（`data-style="color-chip"`）に倣う。2px の枠と、左端 8px の色帯（`inset` の `box-shadow`）。角丸は付けない。

種類は「帯と枠の色」と「見出し語（エラー／完了／お知らせ）」の**2つ**で示す。色を落としても意味が伝わることが条件。**見出しの文字色は種類によらず濃い文字色**（公式も色を付けていない）。

- 各画面の見出しの直後に置く（見出しより前に出すと、どの画面にいるか分からなくなる）
- 処理結果は `role="status"`、操作を止める失敗は `role="alert"`
- `.scanner-status` は同じ色帯の作りだが、常時出るものなので外周は 1px の薄線に留める

---

## 3. 文体

公的サービスの案内文に近い簡潔な日本語で書く。

**使わない**: 「さあ、はじめましょう！」のような煽り／「スキャンしてみよう」／「おっと！」／絵文字／感嘆符／不要な英語見出し／広告的なキャッチコピー

**エラー文は「何が起きたか。次に何をすればよいか。」の順。** 原因だけ書いて終わらせない。

```
書誌情報が見つかりませんでした。ISBNを確認して、もう一度読み取ってください。
カメラへのアクセスが許可されていません。ブラウザの設定を確認して、カメラの使用を許可してください。
ネットワークに接続されていません。接続を確認して、もう一度実行してください。
```

例外の文面は `App.tsx` の `describeError()` に集約してある。新しい失敗を増やすときはここに足す。
カメラ関連の文面は `lib/barcode.ts` にあり、`lib/barcode.test.ts` で固定している。

---

## 4. 整えるべき画面状態

ハッピーパスだけ作って終わりにしない。**色だけで状態を伝えず、必ず文章を伴わせる。**

| 状態 | どこに出るか |
|---|---|
| 初期状態（カメラ停止） | `.scanner-status` = 「カメラは停止しています」＋ 停止した `.scanner-view` |
| カメラ起動中 | 同 = 「カメラを起動しています」 |
| バーコード探索中 | 同 = 「バーコードを探しています」 |
| ISBN読取成功 | 同 `data-kind="success"` = 「ISBNを読み取りました」+ ISBN |
| 同じISBNの重複 | 同 `data-kind="duplicate"` = 「このISBNは登録済みです」 |
| カメラ権限拒否 | `.notice--error` + 「再読み取り」ボタン |
| 書誌情報の取得中 | `.scan-feed li[data-state="looking"]` と、一覧上の「読み取った本を調べています」 |
| 書誌情報の取得成功 | `.scan-feed` に書名が入る。一覧の行にも書名・著者・出版社が入る |
| 書誌情報なし | `.scan-feed li[data-state="missing"]` と `.record__state[data-kind="error"]`、および取得し直しの案内 |
| 通信エラー | `.notice--error`。原因と次の行動の順で書く |
| オフライン | `.notice--error`（`useOnline`）。取得し直しのボタンも無効にする |
| 蔵書一覧が空 | `.panel` の案内 |
| 削除確認 | `.confirm`（行内に留め、前面に浮かせない） |
| 全件削除の確認 | 同（「保存データの削除」の節）|

背表紙の読み取り（`SpineScanner`）はこれに加えて次を持つ。**読めない理由は必ず個別の文にする。**
「読み取れません」だけでは、利用者は次に何をすればよいか判らない。

| 状態 | どこに出るか |
|---|---|
| OCRの準備中 | `.scanner-status` = 「文字の読み取りを準備しています」（初回は数MBを読み込む） |
| 背表紙の探索中 | 同 = 「背表紙を探しています」+ 中央の `.scanner-lane` |
| 暗い / 反射 / ぼやけ | 同 = 「読み取れる状態ではありません」+ それぞれ別の対処文（`capture.ts` の `frameAdvice`） |
| 取り込み成功 | 同 `data-kind="success"` = 「背表紙を取り込みました」 |
| 同じ背表紙の重複 | 同 `data-kind="duplicate"` = 「この背表紙は取り込み済みです」 |
| 処理が追いつかない | 同 = 「読み取りが追いついていません」+ 動かす速さを落とす案内 |
| 進み具合 | `.status-line` = 取り込み枚数 / 文字の読み取り待ち / 書誌情報の取得待ち / 読み取れず |
| OCRが使えない | `.notice--error`（言語モデルを取得できなかった等） |
| 要確認 | `.scan-feed li[data-state="review"]` と、一覧行の候補一覧 + `.crop`（読み取った背表紙） |
| 読み取り文字の修正 | `.record__edit`（行内のフォーム。ラベル → 補足説明 → 入力欄の順） |
| バーコードによる確定 | `.record__actions` の「バーコードで確定」→ 見出しが「バーコードで確定する」に変わる |

---

## 5. アクセシビリティの下限

WCAG 2.2 AA を目標とする。

- 本文と背景 4.5:1 以上、UI部品と背景 3:1 以上
- タップ領域 44×44px 以上（`--tap-min`）。ラジオ・チェックボックスは包む `<label>` で確保する
- キーボードで到達・操作できる。フォーカスは `:focus-visible` の輪（黄 + 濃色の外周）で必ず見える
- `input` / `button` に `label` または accessible name がある
- 読取結果とエラーに `aria-live`（`role="status"` / `role="alert"`）を付ける
- `prefers-reduced-motion: reduce` に対応する
- **320px 幅でも横スクロールを発生させない。** `overflow-x: hidden` で隠して済ませない

---

## 6. やらないこと

グラデーション／glassmorphism／ネオン色／紫を中心にしたAIサービス風配色／大きなヒーローセクション／装飾目的のイラスト／キラキラ・魔法・AIを連想させるアイコン／すべてをカードで囲むレイアウト／過剰な角丸／常時表示される大きな影／多数の色付きバッジ／意味のないアニメーション／ホバーで大きく浮き上がる表現／スマートフォンで場所を取る説明文／アイコンだけで意味を伝えるボタン

配色は1つだけ持つ（`color-scheme: light`）。**ダークテーマを併設しない** — 検証点が倍になり、コントラストの担保が崩れやすい。

---

## 7. 変更したら確認すること

```
npx tsc -b        # 型
npm test          # 単体テスト
npm run build     # 本番ビルド
```

画面に手を入れたときは、加えて次を確認する。

- 幅 **320 / 375 / 390px** と PC幅で崩れないこと、横スクロールが出ないこと
- 14px 未満の文字が増えていないこと
- 押せるものが 44px を下回っていないこと
- 文字と背景のコントラストが 4.5:1（大きい文字は 3:1）を下回っていないこと
- ハッピーパス以外（第4節の表）が残っていること

---

## 8. 依頼時の指定から変えた値

最初の依頼では色と角丸が数値で指定されていたが、公式実装を読んだ結果いくつか差があった。**公式の値を採用してある。** 元の指定に戻したい場合はここを見て `:root` を書き換える。

| 項目 | 依頼時の指定 | 採用した公式値 | 理由 |
|---|---|---|---|
| エラー | `#b10e1e` | `#ec0000` | `semantic-error-1`。`#b10e1e` は DADS に無い |
| 成功 | `#006e54` | `#197a4b` | `semantic-success-2`。`#006e54` は DADS に無い |
| フォーカス | `#ffd43b` | `#ffd43d` | `yellow-300`。1桁違い |
| ページ背景 | `#f7f7f7` | `#f2f2f2` | `solid-gray-50` |
| 薄い境界線 | `#d9d9d9` | `#949494` | DADS は区切り線に `solid-gray-420` のみを使う |
| 境界線 | `#949494` | `#666666` | 入力欄の枠は `solid-gray-600` |
| リンク | プライマリーと同じ `#0017c1` | `#00118f`（hover で `#0017c1`） | リンクは `blue-1000` で、hover は明るくなる |
| 角丸 | 4px（上限6px） | 8px | `button[md]` / `input` / `select` / `textarea` がいずれも 8px |
| フォーカスの輪 | 指定なし | 内側 黄2px + 外側 黒4px | 公式の `:focus-visible` をそのまま採用 |

**変えなかったもの**: 本文16px・行高1.6（公式のコンポーネントは 1.7 だが、依頼の指定を優先）、フォント指定（外部Webフォントを必須にしない方針を優先し、`Noto Sans JP` は端末にあれば使う）、余白の6段階、コンテンツ最大幅 720px。
