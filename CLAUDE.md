# 本棚スキャナ — 決定事項

新規セッションの引き継ぎ用。**確定した判断とその理由**だけを書く。
手順や使い方は [README.md](README.md)、設計の背景は [docs/CONCEPT.md](docs/CONCEPT.md)。
**画面に手を入れるときは [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md) を先に読む**（依頼原文は [docs/DESIGN_RULES.md](docs/DESIGN_RULES.md)）。

- 公開URL: <https://popy48771-collab.github.io/bib/>
- `main` に push すると GitHub Actions が Pages へ自動デプロイする
- 検査: `npm test`（276件）/ `npx tsc -b` / `npm run build`

---

## 1. 全体の制約

**GitHub Pages = 静的ホスティングであり、秘密を持てない。** この1点が設計の大半を決めている。

- 外部APIはブラウザから直接叩く。したがって**呼び先が CORS 対応であることが必須条件**
- 蔵書データはすべて端末内（IndexedDB）。こちら側に一切残さない

## 2. 操作は「かざすだけ」。押すボタンは1つ

**設定画面を持たない。設定項目が1つでもあると「まず設定してから使うもの」に見え、
かざすだけという性質が濁る。** APIキー・プロキシURL・地域コードの入力欄はすべて廃した。
必要な値（NDL中継URL・Google Books の地域）はコードに定数として埋め込んである（`types.ts`）。

**カメラを起動したら、あとは自動。** 読み取り・照合・一覧化にボタンを挟まない。

| 方式 | 状態 |
|---|---|
| **バーコード**（`ui/BarcodeScanner.tsx`） | 動く。ISBN 完全一致なので確実 |
| **本棚の背表紙**（`ui/SpineScanner.tsx`） | 動く。端末内OCR（Tesseract）。読み落としと要確認が出る |

どちらも `ExtractedSpine` / `entriesFromIsbns`（`types.ts` / `pipeline/stages.ts`）で
**同じ照合パイプラインに合流する。** 読み取り手段を増やすときもこの合流点を使う。

### 削除したもの（復活させる前に読むこと）

- **チャットAI経路** — 写真の添付と結果の貼り戻しが手作業で、「かざすだけ」から外れていた
- **VLM（背表紙の写真）経路** — 設定画面を廃した結果、BYOK のキー入力口が無くなり構成できない。
  復活させるならキーをどこから得るかが先。`@anthropic-ai/sdk` 依存も外してある。
  端末内OCRの精度が足りないと実測できたときの次の手であり、`SpineRecognizer`
  （`lib/spine/recognizer.ts`）を差し替えれば以降の処理は変えずに済む
- **段階ごとの実行ボタン** — 理由は §4

### バーコードの決定

- **かざすだけの連続スキャン。** シャッターを押させると1冊ごとに手が止まり棚卸しの速度が出ない
- **読んだ ISBN はその場で App に渡す。** 溜めて最後に一括で渡すと「読み終えるボタンを押すまで
  一覧が空」という状態が生まれる。1冊ごとに行が増え、書名が後から埋まる
- **978/979 で始まるものだけを ISBN として採用する。** 日本の書籍は2段バーコードで、下段(192...)は分類・価格コード。**下段も EAN-13 なのでチェックディジットは正しく通ってしまう**ため、検査数字だけの判定では書籍IDでない数字を取り込む
- 検出器は `BarcodeDetector`（内蔵・高速）優先、非対応（iOS Safari 等）は `zxing-wasm` に退避。**wasm は初回スキャンまで遅延ロード**（本体バンドルへの影響を7kBに抑え、1MBのwasmは別チャンク）
- `getUserMedia` はセキュアコンテキスト必須。**素の http では必ず失敗する**ので、原因の分かるメッセージを出す
- **走査ループの useEffect に props を依存させない。** 1冊読むたびに `knownIsbns` も `onIsbn` も
  作り直されるので、依存に入れると読み取るたびにカメラが開き直る。値は ref 経由で渡す

### 背表紙の決定

**画面内の背表紙を同時に切り分けない。中央に1冊ぶんの「読取レーン」を置き、棚に沿って流させる。**
棚全体から背表紙の境界を出すには直線検出が要り、OpenCV.js（8MB超）を持ち込むことになる。
レーン方式なら境界検出そのものが不要で、1冊ぶんに解像度を割け、Canvas 2D だけで完結する
（`lib/spine/capture.ts`）。棚一段の一括撮影は、この方式で精度を測ったあとの拡張とする。

- **レーンの枠と、実際に切り出す範囲を一致させる。** バーコードの映像は `object-fit: cover` で
  4:3 に埋めているが、背表紙で同じことをすると映像の左右が隠れ、「枠に入れたのに読まれない」が起きる。
  背表紙側は映像の縦横比のまま出す（`.scanner-view--spine`）。枠の % は `LANE_WIDTH_RATIO` と対
- **毎コマ OCR しない。** 縮小した中央領域を 200ms ごとに調べ、明るさ・白飛び・ブレ・動きが
  条件を満たしたときだけ原寸で3枚連写し、**最も鮮鋭な1枚**だけを OCR へ回す
- **止まらない人からも取り込む。** 「静止したら撮る」だけだと、棚に沿って途切れなく動かす人からは
  1枚も取れない。品質が足りているコマが 2.5 秒続いたら、静止していなくても取り込む
- **重複抑止は二段階。** OCR の前に画像ハッシュで弾き（処理そのものを起こさない）、OCR の後に
  文字列の類似度で束ねる。**どちらも「直近数秒」に限る。** 棚の離れた場所に同じ本が出たときは
  2冊持っている可能性があり、黙って消してはならない（`lib/spine/tracker.ts`）
- **OCR は画面の外に置く。** カメラを閉じた時点で Worker ごと落とすと、待ち行列に残った本が消える。
  `ui/useSpineScan.ts` が持ち、待ち行列を捌き終えてから Worker を破棄する
- **待ち行列に上限を置き、溢れたら伝える。** 無制限に溜めると端末の記憶域を食い潰し、
  カメラを閉じてから何分も処理が続く。溢れたら「ゆっくり動かしてください」と出す
- **Worker は1つ。言語は `jpn_vert+jpn` を同時に持たせる。** 複数 Worker はスマートフォンの
  メモリと発熱で落ちる。読むたびに `reinitialize` で言語を切り替える方式は、切り替えのたびに
  モデルを読み直すので棚卸しの速度が出ない
- **読めなければ回して読み直す。** 1周目は縦書き（PSM 5）。駄目なら90度回して横組み（PSM 6）を
  両方向試す。洋書の背表紙は上から下と下から上の両方があり、事前に判らない。
  **1周目で読めた本には2周目以降が走らない**ので、費用がかかるのは読めなかった本だけ
- **OCR の資産は自サイト配下から配る。** tesseract.js は既定で Worker・wasm・言語モデルを
  jsDelivr に取りに行く。静的サイトの可用性が第三者のCDNに乗るので、3つとも自前で持つ
  （wasm は npm 依存を Vite に資産として出させ、言語モデルは `public/ocr/lang/`）
- **`tesseract.js` は本体ではなく配布済みの ESM ビルドを読む。** 本体は CommonJS で node 用の
  分岐（fs / node-fetch）を含み、バンドラの browser フィールド解決に結果が左右される
- **重い資産は遅延ロードする。** 動的 import なので、バーコードしか使わない利用者には降ってこない
  （本体バンドルに tesseract は入っていない。`dist/assets/index-*.js` を grep して確認できる）
- **画像は端末内にだけ残す。** カメラの映像そのものは保存せず、OCR にかけたクロップだけを
  `photos` ストアへ入れる。保存に失敗しても書誌一覧の作成は止めない。
  「すべて削除」は `entries` と `photos` の両方を消す（`db.ts` の `clearAll`）

## 3. 書誌照合の順序

**Google Books は日本語書籍のカバレッジが弱い。** 実際にバーコードで読んだ和書がほぼ全滅した。これは実測で確認済みの事実であり、設計の前提として扱う。

ISBN が読めたら、**当たるまで順に引き、当たった時点で打ち切る**（`resolveEntry`）。

1. **openBD** — ISBN引き専用だが日本の書籍に強く、1リクエストで返る
2. **Google Books** — 和書に弱いが洋書に強い
3. **NDLサーチ** — 法定納本ぶん網羅性が最も高い。最後の砦

**ISBN一致は完全一致なので、タイトル類似度による絞り込みをしない。** 書誌が引けた時点で確定してよく、人間のトリアージを挟まない。これがバーコード経路の値打ちそのもの。同じ理由で、
ISBN 経路では NDL 突合（差分表示）もしない。**完全一致なので比べる相手が無い。**

引けなかったものは「見つからず」として残し、読んだ ISBN は捨てない。UI から再照合できる。

### 書名で引く場合（背表紙経路）は逆に、当たっても打ち切らない

日本語なら **NDL → Google Books**、欧文なら **Google Books → NDL** の順に当て、**必ず両方に当てる。**
片方だけで高得点でも「1つのDBに似た書名の本があった」に過ぎず、確定の根拠にならない（§10.1）。
予算は2つのソースで分け合う（各2回、計4回まで）。先に当てた方が使い切ると、もう片方が
最有力クエリしか撃てなくなり、合意の判定材料が乏しくなる。

**1つの文字列だけで引かない。** 背表紙OCRは、どの行が書名でどの行が著者かを教えてくれない。
最有力行だけで引くと、副題が本題として刷られている本や書名が2行に割れた本を取り逃がす。
当たりやすい順に複数のクエリへ展開し、上から試して当たったところで打ち切る
（`lib/spine/parse.ts` の `buildQueries`）。

**候補の評価も、最有力行との類似度だけで測らない。** 組み立てた全クエリとの最大類似度で測る。
役割の推定を外したときに、正解が閾値で切り捨てられるため（`scoreSpineCandidates`）。

**確定したものにだけ openBD を足す。** openBD はタイトル検索ができないので照合には使えないが、
ISBN が判ったあとの書影・出版社・内容紹介の充実度は日本語書籍で最も高い。

> openBD は2023年に JPRO からのデータ供給が停止し収録範囲が縮小している。
> 現在のコードは提供終了が告知された v1 エンドポイントを叩いており、**実地未確認**。

## 4. 段階ボタンを外した理由

かつては「照合する」「突合する」「補完する」を利用者に押させていた。やめた。

**バーコードで読んだ本には、人間が判断できることが何も無い。** ISBN は完全一致で引けるので、
出てきた書誌をそのまま採るしかない。それでもボタンを押させると「読んだのに一覧が空」という
状態が生まれ、押し忘れれば成果はゼロになる。

**ただし段階分割が持っていた性質は全部残す**（`pipeline/stages.ts` 冒頭にも記載）。

1. **冪等** — 同じ項目を再照合しても壊れない
2. **非破壊** — 後のソースは前の候補を消さない。空振りした記録も残す
3. **隔離** — あるソースが落ちても例外を飲んで次へ回す。1冊の失敗が他へ波及しない
4. **尊重** — 利用者が手で確定したもの（`pinned`）と除外したものは自動処理で触らない

照合は**直列キューで1冊ずつ**流す。同時に何本も投げると Google Books に絞られる。

### 背表紙経路だけは、確定の条件を厳しくする

バーコードと違い、背表紙の読み取りは**「それらしいが存在しない本」を出す。**
したがって OCR の自己申告信頼度を確定の根拠に入れない。「読めた」と「実在する本と一致した」は
別のことである。自動で確定してよいのは次のどちらかだけ（`statusFromEvidence`）。

1. **複数の書誌ソースが同じ ISBN を指した**
2. **正規化した書名がほぼ完全一致し、著者も一致した**

読めた文字が短すぎるもの（正規化して4文字未満）は、当たっても確定させない。
「猫」だけ読めて偶然当たった、を確定にしてはならない。
似た書名で ISBN が違うものは版違いの疑いとして `conflict` にする。

**最優先の指標は自動確定精度である。** 読み落としが多少あっても、誤った本を確定一覧へ混ぜない。

## 5. NDL 中継（動作確認済み）

**日本の書籍は法定納本により NDL の網羅性が最も高い。** 「必ずヒット」に最も近い唯一の手段。

**NDLサーチ API は CORS 非対応。** ブラウザから直接呼べないため中継が要る。

- 中継: Cloudflare Workers（`proxy/ndl-worker.js`）。**秘密情報を持たない**ので鍵の管理が不要
- **URLはアプリに定数として埋め込んである**（`types.ts` の `NDL_PROXY_URL`）。差し替えの設定画面は持たない
- 埋め込みに伴い**呼び出し元を制限**。URLは公開サイトのJSに載って誰にでも見えるため、`ALLOWED_ORIGINS` に無い生成元は403で断る。Origin ヘッダが無いもの（直接アクセス・curl）は通す（NDLを直接叩くのと変わらず、防ぐ意味がない）
- 中継先は `ndlsearch.ndl.go.jp` に限定。踏み台にさせない
- 書影URL（`thumbnailUrl`）だけは中継が要らない。**CORS が制限するのは fetch であって `<img>` ではない**

## 6. データモデル

**1冊を「確定した1レコード」として持たない。** 各ソースからの候補を並列に保持し、どのフィールドがどのソース由来かを `provenance` で追う（`types.ts`）。

**バーコード由来かどうかは `rawText` で判る。** 照合が一度通ると `provenance.isbn13` は書誌ソース側へ
移るので、これだけを見ると再照合が書名検索に落ちてしまう。`entriesFromIsbns` が `rawText` に
読んだ ISBN を残しており、`knownIsbnOf` がそれを退避経路にしている。

## 7. 画面の決定

規則の全文は [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)。ここには「なぜそうしたか」だけを残す。

- **図書館の検索システムや行政の手続画面に寄せる。** 蔵書データを扱う道具なので、装飾より正確さと静かさを優先する。**ロゴ・紋章・政府公式サービスと誤認させる表現は使わない**（フッターで公式でない旨を明示している）
- **画面の値は目分量で決めず、デジタル庁デザインシステムの公式実装から採る。** 出所は `@digital-go-jp/design-tokens`（npm）と `digital-go-jp/design-system-example-components-html`（GitHub）。どちらもこの環境から取得できる（figma.com と design.digital.go.jp は egress で遮断されているが、npm と github.com は通る）
- **色・角丸・影は `src/ui/styles.css` 冒頭の `:root` だけで定義する。** 画面側で個別の値を足すと、次の改修で必ず崩れる。各トークンには対応する公式トークン名をコメントで併記してある
- **区切り線を薄くしすぎない。** DADS が `divider`/`table`/`list` で使う線は `solid-gray-420`(#949494) だけで、`gray-100` や `gray-200` を線に使う例は無い
- **配色は1つだけ持つ**（`color-scheme: light`）。ダークテーマを併設すると検証点が倍になり、コントラストの担保が崩れやすい
- **画面は1枚のまま。** 読み取りと一覧が同じ画面で伸びていく作りなので、画面遷移を挟むと「読めたのか」が見えなくなる。ルータも入れない（**ベースパスに依存しないため**）
- **状態は色だけで伝えない。** 通知も一覧の行も、必ず文章を伴わせる
- **書影の枠は常に確保する。** 取得できた行だけ幅が出ると、書名の頭が行ごとにずれて一覧として読めない
- **カメラのエラー文は `lib/barcode.test.ts` で固定した。** 実機の権限拒否（`NotAllowedError`）はヘッドレスブラウザでは再現できず（`NotSupportedError` になる）、E2Eでは守れない
- **背表紙の画面でも「読めない理由」を文で出す。** 暗い・反射している・ぼやけている・追いついていない、をそれぞれ別の文にしてある（`capture.ts` の `frameAdvice`）。「読み取れません」だけでは、利用者は次に何をすればよいか判らない
- **読み取ったクロップは、確認が要る行にだけ出す。** 候補を選ぶとき本棚まで見に戻らずに済む。確定した行に出しても場所を取るだけ
- **救済の導線は行の中に置く。** 「読み取り文字を修正」と「バーコードで確定」はどちらも `.record__actions`。**バーコードで確定しても行は増やさない**（新しい行を作ると、同じ本が2行に増えて棚卸しの結果が狂う）

## 8. 時間を失った落とし穴

- **`tsc` が `src/` に `.js` を吐くと Vite が `.tsx` より先にそれを解決し、以降の編集がビルドに反映されなくなる。** 症状はバンドルのハッシュが変わらないこと。`package.json` の `typecheck` は `tsc -b` のみにしてある（`--noEmit false` を付けてはならない）
- **zxing-wasm のフォーマット名は `EAN13`**。`EAN-13` は無効で、エラーにならず全フォーマット走査に落ちるだけなので気づけない
- **非同期の照合結果を state のクロージャ経由で書かない。** 1冊ずつ順不同に返るので、古い配列を
  掴んで先に返った結果を消す。書き込みは `entriesRef` を通す（`App.tsx`）
- **GitHub Pages の初回有効化は手作業。** Actions の既定トークンには Pages を作成する権限が無く、`enablement: true` でも `Resource not accessible by integration` で失敗する
- **ルート直下の `vite.config.js` / `vitest.config.js` を復活させない。** Vite も Vitest も `.js` を `.ts` より先に読む。以前 `tsc` が吐いた `.js` が残っており、`.ts` を編集しても反映されない状態だった（同じ症状が `src/` でも起きる。上の項を参照）。両方とも削除済み
- **白飛び判定は「明るい画素」ではなく「振り切れた画素」を数える。** 閾値を 250 に置いたところ、**白い紙のカバーが全部「反射しています」で弾かれた**（実際に E2E で1枚も取り込めなかった）。潰れているのは 254 以上で、そこまで来ると文字は残っていない（`BLOWOUT_LEVEL`）
- **画像ハッシュだけで重複を止めない。** 8x8 の平均ハッシュは、のっぺりした背表紙では平均前後の画素が揺れて距離が開く。同じ本を映したままでも、しばらくすると別物として通ってしまう。**OCR 後の文字列による抑止が実際の受け皿になっている**（二段構えにしてある理由）
- **OCR 後の重複抑止を「時間」で切ってはならない。** 最初 8 秒の窓にしたところ、**同じ本が3行に増えた。** OCR は1冊あたり数秒かかり、待ち行列が伸びると、続けて撮った2枚が10秒以上離れて処理される。窓が先に切れる。突き合わせる相手は**「直近に読んだ何冊か」という件数**で持つ（`SpineTracker`）。1段目（レーンに写っているか）は時間でよい
- この開発環境の egress は外部への到達を広く遮断している（jsDelivr も届かない。npm と raw.githubusercontent.com は通る）。**外部APIの実地確認はできない**前提で作業すること。
  UIの通し確認は Playwright で行える。バーコードは `BarcodeDetector` と `fetch` の差し替え、
  背表紙は `navigator.mediaDevices.getUserMedia` を **canvas の `captureStream()`** に差し替えて、
  日本語の縦書きを描いた偽の映像を流す（この環境には IPAGothic が入っている）。
  OCR そのものは本物が動くので、資産の配信元と読み取りの成否まで確認できる

## 9. やらないと決めたこと

- **設定画面を復活させない。** §2 の理由による
- **サブスク（Claude Max / Gemini Pro / ChatGPT Plus）を API 代わりに使わない。** いずれも API を含まず、認証を流用する自動化は規約に触れうる
- **ライブカメラの映像をVLMに毎フレーム投げない。** 棚1枚が30枚分のコストになる
- **OpenCV.js を入れない**（8MB超）。直線検出が本当に必要になるまで

## 10. 次の一手の候補

- **精度の実測（最優先）**。自分の本棚で正解データを作り、次を数字で出す。
  背表紙経路の閾値はすべて「実機で調整する前提の初期値」であり、いまは根拠が無い
  - 捕捉率（物理的な背表紙のうちクロップを作れた割合）
  - 候補再現率@1 / @3
  - **自動確定精度**（`confirmed` のうち正解だった割合）— 目標 99%以上
  - 連続コマ由来の重複率、1分あたりの処理冊数、端末の発熱
- **閾値の調整**。実測の結果で `types.ts` の `SPINE_*` と `capture.ts` の品質判定を動かす。
  ここを動かすときは、**自動確定精度を落とさない方向にだけ**動かす
- **洋書用の `eng` 言語モデル**。`jpn` でどこまで読めるかを測ってから足す（`public/ocr/README.md`）
- **棚一段の一括撮影**（設計書の Phase 3）。中央レーン方式で精度が確認できてから。
  縦方向のエッジ投影で背表紙の境界を出す。OpenCV.js を入れるかはこの段階で判断する
- Zotero への直接書き込み（Web API は CORS 対応済み。現状は BibTeX 書き出し→読み込みで実用可能）
