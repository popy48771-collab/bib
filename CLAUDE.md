# 本棚スキャナ — 決定事項

新規セッションの引き継ぎ用。**確定した判断とその理由**だけを書く。
手順や使い方は [README.md](README.md)、設計の背景は [docs/CONCEPT.md](docs/CONCEPT.md)。
**画面に手を入れるときは [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md) を先に読む**（依頼原文は [docs/DESIGN_RULES.md](docs/DESIGN_RULES.md)）。

- 公開URL: <https://popy48771-collab.github.io/bib/>
- `main` に push すると GitHub Actions が Pages へ自動デプロイする
- 検査: `npm test`（167件）/ `npx tsc -b` / `npm run build`

---

## 1. 全体の制約

**GitHub Pages = 静的ホスティングであり、秘密を持てない。** この1点が設計の大半を決めている。

- 外部APIはブラウザから直接叩く。したがって**呼び先が CORS 対応であることが必須条件**
- APIキーが要るものは **BYOK**（利用者のキーを localStorage に保存）
- 蔵書データ・写真はすべて端末内（IndexedDB）。こちら側に一切残さない

## 2. 読み取りは3方式。利用者が選ぶ

どれが良いかは状況で変わるので、1つに決めない。**既定はバーコード**（APIキー不要・課金なしで初見でも必ず動くため）。

| 方式 | 課金 | 範囲 | 状態 |
|---|---|---|---|
| **バーコード**（`ui/BarcodeScanner.tsx`） | なし | 1冊ずつ | 既定 |
| **チャットAI**（`ui/ChatImportPanel.tsx`） | なし | 棚ごと | 貼り付け手動 |
| **背表紙の写真**（`sources/vlm.ts`） | 従量 | 棚ごと | APIキー必要 |

3方式とも**同じ書誌パイプラインに合流する**（`pipeline/stages.ts`）。

### バーコードの決定

- **かざすだけの連続スキャン。** シャッターを押させると1冊ごとに手が止まり棚卸しの速度が出ない
- **978/979 で始まるものだけを ISBN として採用する。** 日本の書籍は2段バーコードで、下段(192...)は分類・価格コード。**下段も EAN-13 なのでチェックディジットは正しく通ってしまう**ため、検査数字だけの判定では書籍IDでない数字を取り込む
- 検出器は `BarcodeDetector`（内蔵・高速）優先、非対応（iOS Safari 等）は `zxing-wasm` に退避。**wasm は初回スキャンまで遅延ロード**（本体バンドルへの影響を7kBに抑え、1MBのwasmは別チャンク）
- `getUserMedia` はセキュアコンテキスト必須。**素の http では必ず失敗する**ので、原因の分かるメッセージを出す

### チャットAI経路の決定

- **URLパラメータに依存しない。** Claude は `?q=` で入るが ChatGPT はモバイルで反映されない報告があり、Gemini には公式の手段が無い。そこで**リンクを開くときは必ずクリップボードにも入れる**
- **画像はURLに載せられない。** 写真の添付だけは人の手で行う
- 取り込みは寛容に。JSON（フェンス付き・素の配列・日本語キー）も箇条書きも ISBN の羅列も受ける。**JSONの切り出しは括弧の深さで行う**（書名に `}` が含まれても終端を誤認しない）

### 背表紙(VLM)経路の決定

- **VLMの出力を最終結果として扱わない。** 存在しない本を発明しうるため、出力は「照合クエリ」として扱い、**書誌DBで実在確認が取れたものだけを確定**させる
- タイル分割は**未実装**。写真1枚をそのまま1リクエストで送る。したがって**棚1段を画面いっぱいに撮ることが必須条件**（棚全体を引きで撮ると解像度不足で読めない）
- 対応プロバイダは Anthropic と Gemini のみ。**OpenAI は未対応**（`Settings['vlmProvider']` に無い）

## 3. 書誌照合の順序

**Google Books は日本語書籍のカバレッジが弱い。** 実際にバーコードで読んだ和書がほぼ全滅した。これは実測で確認済みの事実であり、設計の前提として扱う。

ISBN が判っている項目（バーコード経路）は次の順で当てる。

1. **openBD をまとめて引く**（1リクエストで50件）。ISBN引き専用だが日本の書籍に強い
2. 外れたものだけ **Google Books** に回す
3. それでも駄目なら **NDL 突合**（段階3・ボタン起動）

**ISBN一致は完全一致なので、タイトル類似度による絞り込みをしない。** 書誌が引けた時点で確定してよく、人間のトリアージを挟まない。これがバーコード経路の値打ちそのもの。

openBD 段階（段階4）は**救済にも使う**。書名が未取得の項目には openBD のレコードを全面採用して確定させる。空欄補完だけだと、書名の無いまま一覧に残り続ける。

> openBD は2023年に JPRO からのデータ供給が停止し収録範囲が縮小している。
> 現在のコードは提供終了が告知された v1 エンドポイントを叩いており、**実地未確認**。

## 4. NDL 中継（動作確認済み）

**日本の書籍は法定納本により NDL の網羅性が最も高い。** Google Books や openBD で当たらない本もここで引ける。「必ずヒット」に最も近い唯一の手段。

**NDLサーチ API は CORS 非対応。** ブラウザから直接呼べないため中継が要る。

- 中継: Cloudflare Workers（`proxy/ndl-worker.js`）。**秘密情報を持たない**ので鍵の管理が不要
- **URLはアプリに既定値として埋め込み済み**（`types.ts` の `DEFAULT_NDL_PROXY_URL`）。設定不要で使える
- 埋め込みに伴い**呼び出し元を制限**。URLは公開サイトのJSに載って誰にでも見えるため、`ALLOWED_ORIGINS` に無い生成元は403で断る。Origin ヘッダが無いもの（直接アクセス・curl）は通す（NDLを直接叩くのと変わらず、防ぐ意味がない）
- 中継先は `ndlsearch.ndl.go.jp` に限定。踏み台にさせない
- 既定を入れる前に保存された設定は `ndlProxyUrl` が空文字。**空なら既定に戻す**（`lib/db.ts`）

**NDL 段階は非破壊。** 一次結果を上書きせず、候補を `candidates.ndl` に追加し、差分を `conflicts` に記録し、欠損フィールドだけ補完する。単体テストで保証している。

## 5. データモデル

**1冊を「確定した1レコード」として持たない。** 各ソースからの候補を並列に保持し、どのフィールドがどのソース由来かを `provenance` で追う（`types.ts`）。

段階実行が満たすべき性質（`pipeline/stages.ts` 冒頭にも記載）:

1. **冪等** — 同じ段階を再実行しても壊れない
2. **非破壊** — 後の段階は前の候補を消さない
3. **隔離** — ある段階の失敗が他の成果を壊さない
4. **尊重** — 利用者が手で確定したもの（`pinned`）は自動処理で上書きしない

## 6. 画面の決定

規則の全文は [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)。ここには「なぜそうしたか」だけを残す。

- **図書館の検索システムや行政の手続画面に寄せる。** 蔵書データを扱う道具なので、装飾より正確さと静かさを優先する。参照した原則はデジタル庁デザインシステム。**ロゴ・紋章・政府公式サービスと誤認させる表現は使わない**（フッターで公式でない旨を明示している）
- **色・角丸・影は `src/ui/styles.css` 冒頭の `:root` だけで定義する。** 画面側で個別の値を足すと、次の改修で必ず崩れる
- **配色は1つだけ持つ**（`color-scheme: light`）。ダークテーマを併設すると検証点が倍になり、コントラストの担保が崩れやすい。以前あった暗色系の配色は落とした
- **画面を3つに分けた**（読み取り／蔵書一覧／設定）。1枚に全部を積むと、スマートフォンで主操作が埋もれる。ルータは入れず state で切り替える（**ベースパスに依存しないため**）
- **段階2〜4のボタンは蔵書一覧側に置く。** 対象が一覧の項目なので、結果の隣にある方が分かる
- **段階が完走しても、個別の取得失敗は件数を数えて必ず報告する**（`StageContext.onEntryError`）。段階は「隔離」の性質のため1件の失敗で止まらないが、黙って飛ばすと通信が切れていても成功と見分けられない
- **書影の枠は常に確保する。** 取得できた行だけ幅が出ると、書名の頭が行ごとにずれて一覧として読めない
- **カメラのエラー文は `lib/barcode.test.ts` で固定した。** 実機の権限拒否（`NotAllowedError`）はヘッドレスブラウザでは再現できず（`NotSupportedError` になる）、E2Eでは守れない

## 7. 時間を失った落とし穴

- **`tsc` が `src/` に `.js` を吐くと Vite が `.tsx` より先にそれを解決し、以降の編集がビルドに反映されなくなる。** 症状はバンドルのハッシュが変わらないこと。`package.json` の `typecheck` は `tsc -b` のみにしてある（`--noEmit false` を付けてはならない）
- **zxing-wasm のフォーマット名は `EAN13`**。`EAN-13` は無効で、エラーにならず全フォーマット走査に落ちるだけなので気づけない
- **GitHub Pages の初回有効化は手作業。** Actions の既定トークンには Pages を作成する権限が無く、`enablement: true` でも `Resource not accessible by integration` で失敗する
- この開発環境の egress は外部への到達を広く遮断している。**外部APIの実地確認はできない**前提で作業すること

## 8. やらないと決めたこと

- **サブスク（Claude Max / Gemini Pro / ChatGPT Plus）を API 代わりに使わない。** いずれも API を含まず、認証を流用する自動化は規約に触れうる
- **ライブカメラの映像をVLMに毎フレーム投げない。** 棚1枚が30枚分のコストになる
- **Tesseract.js によるローカルOCR は後回し。** 「棚を撮るだけ」を課金ゼロで実現する唯一の道だが、日本語縦書き背表紙は最悪ケースで投資対効果が最も悪い
- **OpenCV.js を入れない**（8MB超）。直線検出が本当に必要になるまで

## 9. 次の一手の候補

- **精度の実測**（最優先）。自分の本棚で正解データを作り、再現率・適合率を数字で出す。体感でなく数字で議論できて初めて改善が回る
- タイル分割の実装（背表紙経路の精度が足りない場合）
- Zotero への直接書き込み（Web API は CORS 対応済み。現状は BibTeX 書き出し→読み込みで実用可能）
- 写真上のバウンディングボックス表示（`ExtractedSpine.box` は型にあるが未使用）
